<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.praktikum.spapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.appointment.UpdateAppointmentActicity"></activity>
        <activity android:name=".activities.appointment.OpenProjectAppointmentActivity" />
        <activity android:name=".activities.appointment.CreateAppointmentActivity" />
        <activity android:name=".activities.project.OpenMyProjectsActivity" />
        <activity android:name=".activities.project.CreateProjectActivity2" />
        <activity android:name=".activities.project.OpenMailActivity" />
        <activity android:name=".activities.user.ShowUserDetailsActivity" />
        <activity android:name=".activities.user.ShowFetchedUsersActivity" />
        <activity android:name=".activities.user.InviteActivity" />
        <activity android:name=".activities.project.CreateProjectActivity" />
        <activity android:name=".activities.general.WelcomeActivity" /> <!-- <activity android:name=".Login"> -->
        <meta-data
            android:name="com.google.android.actions"
            android:resource="@xml/login2" />

        <activity
            android:name=".activities.general.LoginActivity"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.user.CheckForInviteActivity" />
        <activity android:name=".activities.user.CheckForInvite2" />
        <activity android:name=".activities.project.OpenAllProjectsActivity" />

        <meta-data
            android:name="com.google.android.actions"
            android:resource="@xml/login2" />
    </application>

</manifest>